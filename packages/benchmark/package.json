{
  "name": "@sonata/benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The model-vs-model matrix: scenarios x models x seeds, planned, priced, run and rendered.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sonata/core": "*",
    "@sonata/engine": "*",
    "@sonata/judge": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vitest": "^3.0.2"
  }
}
