{
  "name": "@sonata/world",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "One description in, one coherent fake company out — the same cast in Gmail, Slack and Calendar.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./templates": "./src/templates/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sonata/core": "*",
    "openai": "^7.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vitest": "^3.0.2"
  }
}
