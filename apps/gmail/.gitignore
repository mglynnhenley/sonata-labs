# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# data: databases + real Google credentials — never commit
/data/
*.db
*.db-wal
*.db-shm
google-token.json

# misc
.DS_Store
*.log
# secrets: OPENROUTER_API_KEY etc. `.env*.local` alone does NOT cover a plain `.env`
.env
.env*.local
!.env.example
/coverage
.gstack/

# build artifacts
*.tsbuildinfo
