---
category: Overlays
---
Centered dialog with focus trap, Escape and backdrop close (`dismissible={false}` for destructive confirmations). `title`, `description`, `footer` buttons right-aligned, primary last.
