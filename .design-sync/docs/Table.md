---
category: Data
---
Dense data table from a `columns` config (align, width, render). `rowHref` makes rows real links; `onRowClick` adds client routing; numeric cells get tabular figures via `data-numeric`.
