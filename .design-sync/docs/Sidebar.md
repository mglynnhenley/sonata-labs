---
category: Navigation
---
App rail: white, hairline border, `brand` block on top, grouped nav children, `footer` pinned to the bottom. Below `lg` it becomes a drawer driven by `open`/`onClose` with `SidebarTrigger` in the content column.
