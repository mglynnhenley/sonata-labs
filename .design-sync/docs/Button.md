---
category: Actions
---
Pill button. Variants: primary (petrol, one per view), secondary (outlined, the default), ghost, danger. Sizes sm/md/lg; `loading` swaps in a spinner without resizing; `icon`/`iconRight` take an Icon* element.
